export interface PercussionObject {
  kick: boolean[],
  snare: boolean[],
  highHat: boolean[],
  subBass: boolean[],
}


export interface SynthPadBounds {
  top: number,
  right: number,
  bottom: number,
  left: number,
  volumeRange: number,
  pitchRange: number,
}

